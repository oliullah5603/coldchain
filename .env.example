PORT=4310
APP_ORIGIN=http://localhost:4310
# demo: bundled synthetic FHIR + PKCE sandbox; smart: registered external EHR
EHR_MODE=demo
# live calls NLM; snapshot uses explicitly labelled recorded responses for offline demos
RXNORM_MODE=live
# External SMART configuration (not needed for demo)
# FHIR_BASE_URL=https://your-ehr.example/fhir
# SMART_CLIENT_ID=registered-client-id
# SMART_CLIENT_SECRET=server-side-confidential-client-secret
# SMART_ALLOWED_AUTH_ORIGINS=https://your-auth.example
# DATA_DIR=./data
# AUDIT_KEY=provide-at-least-32-random-characters-for-managed-deployments
